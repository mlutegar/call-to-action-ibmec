import React from 'react';
import { HeroContainer, HeroTitle, HeroSubtitle } from './Style';

const HeroSection = () => {
  return (
    <HeroContainer>
      <HeroTitle>E-books</HeroTitle>
      <HeroSubtitle>
        Revistas com dos melhores TCCs de cada semestre do Curso de Publicidade IBMEC
      </HeroSubtitle>
    </HeroContainer>
  );
};

export default HeroSection;